---
// Aviso permanente y visible: todo el contenido de expositores, sponsors,
// oradores y noticias es de demostración (ver restricción del pliego).
// No es descartable por el usuario: tiene que seguir visible en toda
// navegación, así que no lleva botón de cierre.
---

<div class="aviso-demo" role="note">
  <p class="contenedor aviso-demo__texto">
    <strong>Prototipo de propuesta —</strong> Desafío Digital ExpoJuy 2026. Expositores, sponsors, oradores y noticias son contenido de demostración, sin datos reales.
  </p>
</div>

<style>
  .aviso-demo {
    background: var(--color-hueso);
    border-bottom: 1px solid var(--color-borde-ui);
  }

  .aviso-demo__texto {
    padding-block: var(--space-2);
    font-size: var(--texto-sm);
    color: var(--color-texto);
    text-align: center;
  }

  .aviso-demo strong {
    color: var(--color-titulo);
  }
</style>
